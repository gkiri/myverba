openai==1.84.0
manim==0.19.0
manim-voiceover==0.3.7
google-generativeai==0.8.5
ffmpeg-python==0.2.0
aiofiles
aiohttp
python-dotenv
psutil
matplotlib
###################
#pip install -e . #for anim_gemini folder visibility
# sudo apt-get update

#sudo apt-get install -y build-essential python3-dev  pkg-config libcairo2-dev
#sudo apt-get install -y libcairo2-dev pkg-config
# sudo apt-get install -y ffmpeg
#sudo apt-get install -y sox libsox-fmt-all
#conda activate final_manim && conda install -c conda-forge texlive-core dvisvgm -y
#conda activate final_manim && apt update && apt install -y texlive-latex-extra texlive-fonts-extra texlive-latex-recommended texlive-science texlive-fonts-recommended
#conda activate final_manim && apt install -y texlive-latex-base

